# Optional: connect to a live Elasticsearch cluster.
# Leave unset to use built-in mock data (works offline).
ELASTICSEARCH_URL=
ELASTICSEARCH_INDEX=page_views

# Server port (Render sets PORT automatically)
PORT=3001
